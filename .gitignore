# Environment
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/

# Data — media, observations, knowledge (không đẩy lên GitHub)
data/

# Knowledge Vault — symlinks tới data/, chỉ dùng local trong Obsidian
Knowledge Vault/

# Raw assets — ảnh gốc, quá lớn cho git
assets/raw/

# Output — generated files, logs
output/

# macOS
.DS_Store
.AppleDouble

# Logs (optional)
# output/logs/

# IDE
.vscode/
.idea/
# Ignore local Claude Code state (settings.local.json, session cache, etc.)
# but keep .claude/skills/ and .claude/CLAUDE.md.proposed tracked -- plan
# Part 8.1/RS-F1/RS-F4 requires skills + governance diffs live in the repo,
# not just on Harry's machine (found untracked 2026-08-06, Phase 8 audit).
.claude/*
!.claude/skills
!.claude/CLAUDE.md.proposed
.obsidian/
