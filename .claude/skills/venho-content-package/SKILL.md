# venho-content-package

Run a locked CreativeBrief into a draft ContentPackage.

Command:
```bash
venho-growth run brief.json
```

Rules: only LOCKED briefs can generate final packages; M07 remains the only publishing gateway.
