pillars:
  - id: west_lake_discovery
    name: Kham pha Ho Tay
    dna_subject: westlake
    topics:
      - morning at West Lake
      - lake view room
      - quiet Hanoi stay
  - id: room_comfort
    name: Phong gon gang gan ho
    dna_subject: lake_view_room
    topics:
      - lake view morning
      - simple hotel comfort

# Saturday special lane (v3.1 cadence_policy.yaml "special" slot -- Harry's
# T7 addition). daily_cycle._pick_topic() runs every group through
# growth_orchestrator.application.special_lane.select_special_lane_candidate()
# (real priority fallback: seasonal_nature > cultural_event > lifestyle_trend
# > feature_story). No live trend/event feed exists yet, so every group below
# defaults to type: feature_story (loai 4, the mandatory fallback) -- this is
# the honest current state, not a shortcut: when a real seasonal_nature/
# cultural_event/lifestyle_trend source is wired in, add `type: ...` (and
# `verified_by_human: true` for cultural_event) to a group and the existing
# priority logic will prefer it automatically.
special_topics:
  - id: weekend_feature
    name: Cuoi tuan o Tay Ho
    dna_subject: outside
    topics:
      - a quiet weekend morning walk around West Lake
      - weekend brunch view at the hotel rooftop
      - slow weekend at Ven Ho Hotel

